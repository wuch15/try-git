#include <cstdio>
#include <cstdlib>
#include <iostream>
#include <cstring>
#include <algorithm>
#include <string>
#include <vector>
#include <map>
#include <set>

using namespace std;

#define rep(i,n) for((i)=1;(i)<=(n);++(i))
#define rep2(i,x,y) for((i)=(x);i<=(y);++(i))
#define repr(i,x,y) for((i)=(x);i>=(y);--(i))

#define pb push_back
#define mp make_pair

typedef pair<int,int> PII;
typedef long long LL;
typedef vector<int> VI;
typedef vector<PII> VII;

int main()
{
	cout << "Hello World!" << endl << "Hello Git!" << endl;
	return 0;
}
